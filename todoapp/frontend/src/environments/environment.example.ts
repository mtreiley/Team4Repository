// Copy this file to environment.ts (production) and environment.development.ts (development)
// and fill in the values for your local setup.
// These files are gitignored and should never be committed.

export const environment = {
  production: false,        // set to true in environment.ts
  apiBaseUrl: 'http://localhost:8080'  // URL of the Spring Boot backend
};
